
name: Beana CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm ci
    - run: npm run build --if-present
    - name: Deploy to Vercel Action
      uses: BetaHuhn/deploy-to-vercel-action@v1.9.12
      with:
        GITHUB_TOKEN: ${{ secrets.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}}
        GH_PAT: ${{ secrets.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 }}
        VERCEL_TOKEN: ${{ secrets.nmx36ni8l2vj6oF3sLBxVLTL}}
        VERCEL_ORG_ID: ${{ secrets.RhZz8MUxNYlkFUYGDkONEuGj}}
        VERCEL_PROJECT_ID: ${{ secrets.prj_PxhMP9RnWxDJCHSx6CVmDmQAfXJH}}
